import React from "react";
import ReactDOM from "react-dom/client";
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode><div><h1>VIBA Deploy Sample App</h1><p>Health: OK</p></div></React.StrictMode>
);
