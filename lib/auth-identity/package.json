{
  "name": "@viba/auth-identity",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "migrations", "README.md", "manifest.json", "PROVENANCE.md"],
  "scripts": {
    "typecheck": "pnpm --workspace-root exec tsc -p lib/auth-identity/tsconfig.json --noEmit",
    "build": "pnpm --workspace-root exec tsc -p lib/auth-identity/tsconfig.json",
    "test": "node --test lib/auth-identity/test/index.test.mjs"
  }
}
